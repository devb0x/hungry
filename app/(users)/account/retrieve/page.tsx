import {Metadata} from "next"

export const metadata: Metadata = {
	title: 'Retrieve your account',
	description: 'Retrieve your account'
}

const Retrieve = () => {
	return (
		<div>
			retrieve component for forgetting account login/password
		</div>
	)
}

export default Retrieve